{
	"name": "ghloc-extension",
	"version": "1.0.4",
	"repository": "https://github.com/pajecawav/ghloc-extension",
	"author": "pajecawav",
	"license": "MIT",
	"scripts": {
		"start": "web-ext run -s dist -t chromium",
		"start:firefox": "web-ext run -s dist -t firefox-desktop",
		"build": "node build.mjs && web-ext build -s dist --overwrite-dest"
	},
	"devDependencies": {
		"@types/chrome": "^0.0.197",
		"esbuild": "^0.28.0",
		"esbuild-plugin-copy": "^2.1.1",
		"prettier": "^2.7.1",
		"typescript": "^4.8.4",
		"web-ext": "^7.2.0"
	}
}
